# Environment Configuration for Project LOOP

# Database Connection (PostgreSQL or SQLite for local dev)
# For local Postgres / Supabase / Neon:
# DATABASE_URL="postgresql://postgres:password@localhost:5432/loop_db?schema=public"
# For zero-config local file SQLite:
DATABASE_URL="file:./dev.db"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="project_loop_super_secret_session_jwt_key_2026"

# Anthropic Claude API (Optional - Built-in smart AI fallback works out-of-the-box)
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-3-5-sonnet-20241022"

# Application Settings
NODE_ENV="development"
